import { Cormorant, Lato, Nunito, Oswald } from 'next/font/google'
 
export const lato = Lato({
  weight: '700',
  subsets: ['latin'],
})

export const nunito = Nunito({
  weight: '500',
  subsets: ['latin'],
})

export const oswald = Oswald({
  weight: '300',
  subsets: ['latin'],
})